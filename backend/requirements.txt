# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-dotenv==1.2.2

# --- Configuration & validation ---
pydantic==2.13.4
pydantic-settings==2.14.2

# --- Database ---
sqlalchemy==2.0.51
alembic==1.19.0
psycopg[binary]==3.3.4

# --- AI ---
langchain==1.3.14
langchain-openai==1.4.1
langgraph==1.2.10

# --- Testing ---
# pytest >= 9.0.3 required to fix PYSEC-2026-1845 (pytest 8.4.2 vulnerable).
# pytest-asyncio >= 1.0.0 required for pytest 9 compatibility.
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1

# --- Tooling ---
ruff==0.16.1

# --- Pinned transitive deps (security) ---
# msgpack < 1.2.1 vulnerable to CVE-2026-57585 (HIGH).
# setuptools < 78.1.1 vulnerable to CVE-2025-47273 (HIGH, path traversal);
# setuptools < 83.0.0 vulnerable to PYSEC-2026-3447.
msgpack==1.2.1
setuptools==83.0.0

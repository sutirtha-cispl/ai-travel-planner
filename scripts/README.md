# Automation scripts

Automation helpers used during development and deployment.

Backend-specific scripts (e.g. `migrate.py`, `seed_database.py`) will be
added here in later sprints. For now, use the Makefile targets or run
Alembic directly from the `backend/` directory.

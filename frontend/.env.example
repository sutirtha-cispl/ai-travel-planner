# ============================================
# AI Travel Planner - Frontend Configuration
# ============================================
# Copy this file to `.env` to override defaults.

# Base URL of the backend API. The default `/api/v1` is same-origin and is
# proxied to the backend by the Vite dev server and the Docker nginx config.
VITE_API_URL=/api/v1
